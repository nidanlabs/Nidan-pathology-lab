(()=>{
const cfg=window.NIDAN_CONFIG||{};if(!cfg.supabaseUrl||!cfg.supabasePublishableKey||cfg.supabasePublishableKey.startsWith('YOUR_'))return;const db=supabase.createClient(cfg.supabaseUrl,cfg.supabasePublishableKey);const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function verify(){const token=new URLSearchParams(location.search).get('verify');if(!token)return;const page=document.getElementById('page');if(!page)return;page.innerHTML='<div class="panel" style="max-width:720px;margin:40px auto;text-align:center"><h2>Verifying report…</h2><p class="muted">Checking the report authenticity with the laboratory.</p></div>';try{const {data,error}=await db.rpc('nidan_verify_report',{p_token:token});if(error)throw error;const r=Array.isArray(data)?data[0]:data;if(!r){page.innerHTML='<div class="error-card" style="max-width:720px;margin:40px auto"><h2>Report Not Verified</h2><p>This verification code is invalid, expired, or the report has not been released.</p></div>';return;}page.innerHTML=`<div class="panel" style="max-width:720px;margin:40px auto;text-align:center;border-top:5px solid #0f766e"><div style="font-size:54px">✓</div><h1 style="color:#0f766e;margin:0">Report Verified</h1><p style="font-size:18px">This report was released by <b>${esc(r.lab_name)}</b>.</p><div style="margin:25px 0;padding:18px;background:#f0f9f7;border-radius:12px;text-align:left"><p><b>Report Number:</b> ${esc(r.report_number)}</p><p><b>Status:</b> ${esc(r.status)}</p><p><b>Released:</b> ${r.released_at?esc(new Date(r.released_at).toLocaleString('en-IN')):'—'}</p></div><p class="muted small-text">This page confirms report authenticity only. Patient results and personal information are intentionally not displayed.</p><button class="ghost" onclick="location.href=location.pathname">Back to Laboratory Portal</button></div>`;}catch(e){page.innerHTML=`<div class="error-card" style="max-width:720px;margin:40px auto"><h2>Verification unavailable</h2><p>${esc(e.message)}</p></div>`;}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',verify);else verify();
})();
