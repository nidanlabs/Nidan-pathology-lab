(() => {
  const cfg = window.NIDAN_CONFIG || {};
  if (!cfg.supabaseUrl || !cfg.supabasePublishableKey || cfg.supabasePublishableKey.startsWith('YOUR_')) return;
  const client = supabase.createClient(cfg.supabaseUrl, cfg.supabasePublishableKey);
  const page = document.getElementById('page');
  const esc = v => String(v ?? '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const catalogs = {
    CBC:[['HB','Hemoglobin','g/dL'],['WBC','WBC','10^3/µL'],['RBC','RBC','10^6/µL'],['PLT','Platelet','10^3/µL'],['HCT','Hematocrit','%'],['MCV','MCV','fL'],['MCH','MCH','pg'],['MCHC','MCHC','g/dL'],['RDW_CV','RDW-CV','%']],
    LFT:[['TBIL','Total Bilirubin','mg/dL'],['DBIL','Direct Bilirubin','mg/dL'],['ALT','ALT','U/L'],['AST','AST','U/L'],['ALP','ALP','U/L'],['TP','Total Protein','g/dL'],['ALB','Albumin','g/dL']],
    RFT:[['UREA','Urea','mg/dL'],['CREAT','Creatinine','mg/dL'],['URIC','Uric Acid','mg/dL'],['SOD','Sodium','mmol/L'],['POT','Potassium','mmol/L']],
    LIPID:[['TC','Total Cholesterol','mg/dL'],['TG','Triglycerides','mg/dL'],['HDL','HDL','mg/dL'],['LDL','LDL','mg/dL']],
    TSH:[['TSH','TSH','µIU/mL']], WIDAL:[['TO','S. typhi O','Titre'],['TH','S. typhi H','Titre'],['AH','S. paratyphi AH','Titre'],['BH','S. paratyphi BH','Titre']], ESR:[['ESR','ESR','mm/hr']]
  };
  const canEnter = r => ['owner','admin','technician'].includes(r);
  const canVerify = r => ['owner','admin','pathologist'].includes(r);
  const canView = r => ['owner','admin','technician','pathologist','receptionist'].includes(r);
  async function session(){const {data:{session}}=await client.auth.getSession();return session;}
  async function profile(){const s=await session();if(!s)throw new Error('Session expired. Please login again.');const {data,error}=await client.from('profiles').select('tenant_id,role,email').eq('user_id',s.user.id).single();if(error)throw error;return {session:s,profile:data};}
  async function loadOrders(){const {data,error}=await client.from('test_orders').select('id,order_number,status,patient_id,sample_id,created_at').order('created_at',{ascending:false}).limit(50);if(error)throw error;return data||[];}
  async function loadItems(orderId){const {data,error}=await client.from('test_order_items').select('id,test_code,test_name,status,result_data,created_at').eq('order_id',orderId).order('created_at');if(error)throw error;return data||[];}
  async function patient(id){const {data,error}=await client.from('patients').select('patient_id,first_name,last_name,age,sex').eq('id',id).single();if(error)throw error;return data;}
  async function sample(id){const {data,error}=await client.from('samples').select('accession_number,status,specimen_type,patient_id').eq('id',id).single();if(error)throw error;return data;}
  function resultFields(item){const fields=catalogs[item.test_code]||[[item.test_code,item.test_name,'']],old=item.result_data||{};return fields.map(([code,label,unit])=>`<div class="result-field"><label>${esc(label)}<input name="${esc(code)}" value="${esc(old[code]??'')}" placeholder="Enter result" ${['verified','released'].includes(item.status)?'disabled':''}><small>${esc(unit)}</small></label></div>`).join('');}
  async function resultsHome(){const {profile:p}=await profile();if(!canEnter(p.role)&&!canVerify(p.role)){page.innerHTML='<div class="error-card"><h2>Access denied</h2><p>You do not have permission to access Results.</p></div>';return;}page.innerHTML='<div class="page-head"><div><p class="eyebrow">RESULT ENTRY</p><h2>Results</h2><p class="muted">Select an order to enter laboratory results.</p></div></div><div class="panel" id="workflowPanel">Loading test orders…</div>';try{const orders=await loadOrders();page.querySelector('#workflowPanel').innerHTML=orders.length?orders.map(o=>`<button class="workflow-row" data-result-order="${o.id}"><span><strong>${esc(o.order_number)}</strong><small>Patient ID: ${esc(o.patient_id)} · Sample: ${esc(o.sample_id)}</small></span><b>${esc(o.status)}</b></button>`).join(''):'<div class="empty">No test orders yet.</div>';}catch(e){page.querySelector('#workflowPanel').innerHTML=`<div class="error">${esc(e.message)}</div>`;}}
  async function resultOrder(orderId){const {profile:p}=await profile();if(!canEnter(p.role)&&!canVerify(p.role))throw new Error('You do not have permission to enter or verify results.');page.innerHTML='<div class="panel">Loading order…</div>';const orders=await loadOrders(),o=orders.find(x=>x.id===orderId);if(!o)throw new Error('Order not found');const [items,pt,s]=await Promise.all([loadItems(orderId),patient(o.patient_id),sample(o.sample_id)]);page.innerHTML=`<div class="page-head"><div><p class="eyebrow">RESULT ENTRY</p><h2>${esc(o.order_number)}</h2><p class="muted">${esc(pt.patient_id)} — ${esc(pt.first_name)} ${esc(pt.last_name)} · Age ${esc(pt.age)} · ${esc(s.accession_number)}</p></div><button class="ghost" data-workflow="results-home">← Back</button></div><div class="panel"><div class="patient-strip"><strong>${esc(pt.first_name)} ${esc(pt.last_name)}</strong><span>Age ${esc(pt.age)}</span><span>${esc(pt.sex)}</span><span>Sample ${esc(s.accession_number)}</span></div>${items.map(i=>`<form class="result-card" data-result-item="${i.id}"><div class="result-head"><div><span class="code">${esc(i.test_code)}</span><h3>${esc(i.test_name)}</h3></div><span class="status-pill">${esc(i.status)}</span></div><div class="result-grid">${resultFields(i)}</div>${['verified','released'].includes(i.status)?'<p class="muted small-text">Verified result — values are locked.</p>':canEnter(p.role)?'<div class="form-actions"><button class="primary" type="submit">Save Result</button><span class="message result-message"></span></div>':'<p class="muted">You can view this result, but cannot edit it.</p>'}</form>`).join('')}</div>`;page.querySelectorAll('[data-result-item]').forEach(f=>f.onsubmit=e=>saveResult(e,o.id));}
  async function saveResult(e,orderId){e.preventDefault();const {profile:p}=await profile();if(!canEnter(p.role)){alert('You do not have permission to enter results.');return;}const form=e.currentTarget,result={};form.querySelectorAll('input[name]').forEach(i=>{if(!i.disabled)result[i.name]=i.value.trim();});const {error}=await client.from('test_order_items').update({result_data:result,status:'result_entered'}).eq('id',form.dataset.resultItem);const msg=form.querySelector('.result-message');msg.textContent=error?error.message:'Result saved successfully.';if(!error)setTimeout(()=>resultOrder(orderId).catch(x=>alert(x.message)),400);}
  async function verificationHome(){const {profile:p}=await profile();if(!canVerify(p.role)&&!canView(p.role)){page.innerHTML='<div class="error-card"><h2>Access denied</h2><p>You do not have permission to access Reports.</p></div>';return;}page.innerHTML='<div class="page-head"><div><p class="eyebrow">VERIFICATION & RELEASE</p><h2>Reports</h2><p class="muted">Verify completed results, generate and release reports.</p></div></div><div class="panel" id="reportWorkflow">Loading…</div>';try{const orders=await loadOrders(),rows=[];for(const o of orders){const items=await loadItems(o.id);if(items.some(i=>['result_entered','verified','released'].includes(i.status)))rows.push({o,items});}page.querySelector('#reportWorkflow').innerHTML=rows.length?rows.map(({o,items})=>{const done=items.filter(i=>i.status!=='cancelled').length,ver=items.filter(i=>['verified','released'].includes(i.status)).length;return `<div class="workflow-row report-row"><div><strong>${esc(o.order_number)}</strong><small>${ver}/${done} tests verified · Sample ${esc(o.sample_id)}</small></div><div class="workflow-actions">${canVerify(p.role)&&items.some(i=>i.status==='result_entered')?`<button class="primary small-btn" data-verify-order="${o.id}">Verify Results</button>`:''}<button class="ghost small-btn" data-open-report="${o.sample_id}">Open Report</button></div></div>`}).join(''):'<div class="empty">No entered results waiting for verification.</div>';}catch(e){page.querySelector('#reportWorkflow').innerHTML=`<div class="error">${esc(e.message)}</div>`;}}
  async function verifyOrder(orderId){const {profile:p}=await profile();if(!canVerify(p.role)){alert('Only owner, admin or pathologist can verify results.');return;}const items=await loadItems(orderId);for(const i of items.filter(x=>x.status==='result_entered')){const {error}=await client.rpc('nidan_verify_test_item',{p_item_id:i.id});if(error){alert(error.message);return;}}alert('All entered results were verified.');verificationHome();}
  async function openReport(sampleId){const {profile:p}=await profile();if(!canView(p.role)){page.innerHTML='<div class="error-card"><h2>Access denied</h2><p>You do not have permission to view reports.</p></div>';return;}page.innerHTML='<div class="panel">Loading report…</div>';try{const s=await sample(sampleId),pt=await patient(s.patient_id);let {data:report,error}=await client.from('reports').select('*').eq('sample_id',sampleId).order('created_at',{ascending:false}).limit(1).maybeSingle();if(error)throw error;if(!report){if(!canVerify(p.role))throw new Error('Report has not been created yet. Verification is required.');const r=await client.rpc('nidan_create_draft_report',{p_sample_id:sampleId});if(r.error)throw r.error;report=r.data;}const {data:items,error:itemError}=await client.from('test_order_items').select('test_code,test_name,status,result_data,created_at,test_orders!inner(sample_id)').eq('test_orders.sample_id',sampleId).neq('status','cancelled').order('created_at');if(itemError)throw itemError;const released=report.status==='released';page.innerHTML=`<div class="page-head"><div><p class="eyebrow">LABORATORY REPORT</p><h2>${esc(report.report_number||'Draft Report')}</h2><p class="muted">${esc(pt.patient_id)} — ${esc(pt.first_name)} ${esc(pt.last_name)} · Age ${esc(pt.age)} · Sample ${esc(s.accession_number)}</p></div><div class="workflow-actions"><button class="ghost" data-workflow="reports-home">← Back</button><button class="ghost" data-print-report="${report.id}">Print / PDF</button>${canVerify(p.role)&&!released?`<button class="primary" data-release-report="${report.id}">Release Report</button>`:''}</div></div><div class="panel report-paper"><div class="report-brand"><div class="brand-mark">N</div><div><h2>NIDAN PATHOLOGY LAB</h2><p>Laboratory Information System</p></div></div><hr><div class="patient-strip"><span><b>Patient:</b> ${esc(pt.first_name)} ${esc(pt.last_name)}</span><span><b>Age:</b> ${esc(pt.age)}</span><span><b>Sex:</b> ${esc(pt.sex)}</span><span><b>Sample:</b> ${esc(s.accession_number)}</span></div><table><thead><tr><th>Test</th><th>Result</th><th>Unit</th><th>Status</th></tr></thead><tbody>${items.map(i=>{const vals=Object.entries(i.result_data||{});return vals.length?vals.map(([k,v])=>{const spec=(catalogs[i.test_code]||[]).find(x=>x[0]===k);return `<tr><td>${esc(i.test_name)} — ${esc(spec?spec[1]:k)}</td><td><b>${esc(v)}</b></td><td>${esc(spec?spec[2]:'')}</td><td>${esc(i.status)}</td></tr>`}).join(''):`<tr><td>${esc(i.test_name)}</td><td>—</td><td></td><td>${esc(i.status)}</td></tr>`}).join('')}</tbody></table><p class="muted small-text">Reference intervals are laboratory/method specific and should be configured before clinical use.</p><div class="report-sign"><div>Verified by<br><strong>${report.verified_by?'Authorized user':'Pending'}</strong></div><div>Released<br><strong>${released?'Released':'Draft'}</strong></div></div></div>`;}catch(e){page.innerHTML=`<div class="error-card"><h2>Unable to open report</h2><p>${esc(e.message)}</p></div>`;}}
  async function releaseReport(reportId){const {profile:p}=await profile();if(!canVerify(p.role)){alert('Only owner, admin or pathologist can release reports.');return;}if(!confirm('Release this report? Released results should not be edited casually.'))return;const {data,error}=await client.rpc('nidan_release_report',{p_report_id:reportId});if(error){alert(error.message);return;}alert(`Report ${data.report_number||''} released successfully.`);openReport(data.sample_id);}
  async function printReport(reportId){const {profile:p}=await profile();if(!canView(p.role)){alert('You do not have permission to print reports.');return;}if(window.NIDAN_PRINT?.report)return window.NIDAN_PRINT.report(reportId);alert('Print module is loading. Please try again.');}
  document.addEventListener('click',e=>{const r=e.target.closest('[data-result-order]');if(r)return resultOrder(r.dataset.resultOrder).catch(x=>page.innerHTML=`<div class="error-card"><h2>Unable to open result</h2><p>${esc(x.message)}</p></div>`);const v=e.target.closest('[data-verify-order]');if(v)return verifyOrder(v.dataset.verifyOrder);const o=e.target.closest('[data-open-report]');if(o)return openReport(o.dataset.openReport);const rel=e.target.closest('[data-release-report]');if(rel)return releaseReport(rel.dataset.releaseReport);const pr=e.target.closest('[data-print-report]');if(pr)return printReport(pr.dataset.printReport);const wh=e.target.closest('[data-workflow]');if(wh){if(wh.dataset.workflow==='results-home')resultsHome();if(wh.dataset.workflow==='reports-home')verificationHome();}});
  document.addEventListener('click',e=>{const nav=e.target.closest('.nav');if(!nav)return;const s=nav.dataset.section;if(s==='results')setTimeout(resultsHome,0);if(s==='reports')setTimeout(verificationHome,0);});
  window.NIDAN_WORKFLOW={resultsHome,verificationHome,openResultOrder:resultOrder};
})();
