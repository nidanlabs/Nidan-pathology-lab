[buildout]
# NIDAN buildout foundation.
# SENAITE package pins and deployment parts will be added in the next phase.
extends =
versions = versions

[versions]
# Keep dependency pins explicit here once the SENAITE 2.7.x buildout is assembled.
